import { RESPONSE } from '../lang/en-US/main.js';

export const name = "com.neuelogic.addr.LOOKUP_BY_NAME";
export const handler = (app) => {
	/*
		const args = {
			name: app.getArgument('name'),
			fieldName: app.getArgument('fieldName')
		};
	*/
};
